//null undefind

let variable;
console.log(variable);

variable = null;
console.log(variable);

let activeItem; //아직 확성화된 아이템이 있는지 없는지 모르는 상태 
activeItem = null; // 이건 확실히 아무것도 없다고 정의한 것  (object type임)
